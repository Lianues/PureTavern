# Upstream source

This directory contains an unmodified copy of the `public/` directory from:

- Project: SillyTavern
- Version: 1.18.0
- Upstream repository: https://github.com/SillyTavern/SillyTavern
- Local reference snapshot: `SillyTavern-1.18.0/public/`
- License: AGPL-3.0; see `UPSTREAM_LICENSE`

The copied upstream files are tracked by `../../legacy-files.sha256`. This source note and `UPSTREAM_LICENSE` are local provenance files and are not part of that upstream hash manifest.

Do not edit upstream files in this directory. Compatibility behavior belongs in `apps/web/src/legacy-host/`.
