{
  "name": "@pure-tavern/mobile",
  "version": "0.1.1",
  "private": true,
  "scripts": {
    "test": "node scripts/verify-file-saver.mjs && node scripts/verify-ios-shell.mjs && node scripts/verify-gradle-runner.mjs",
    "brand:mobile": "python scripts/generate_mobile_branding.py",
    "sync:android": "cap sync android",
    "open:android": "cap sync android && cap open android",
    "build:android:debug": "cap sync android && node scripts/run-gradle.mjs assembleDebug",
    "build:android:release": "cap sync android && node scripts/run-gradle.mjs assembleRelease",
    "sync:ios": "cap sync ios",
    "open:ios": "cap sync ios && cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "8.4.2",
    "@capacitor/core": "8.4.2",
    "@capacitor/ios": "8.4.2"
  },
  "devDependencies": {
    "@capacitor/cli": "8.4.2"
  }
}
